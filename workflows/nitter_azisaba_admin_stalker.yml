on:
  rss:
    url: https://nitter.net/search/rss?f=tweets&q=list%3A1326169415851810818+exclude%3Aretweets
    config:
      skipFirst: true
      sort:
        pubDate: 1
jobs:
  request:
    name: Make a HTTP Request
    runs-on: ubuntu-latest
    steps:
      - name: Make a HTTP Request
        uses: actionsflow/axios@v1
        with:
          url: ${{ secrets.NITTER_AZISABA_ADMIN_STALKER_DISCORD_WEBHOOK }}
          method: POST
          body: |
            {
              "username": "Actionsflow",
              "avatar_url": "https://avatars2.githubusercontent.com/u/69839062?v=4",
              "content": "${{ on.rss.outputs.link }}"
            }
